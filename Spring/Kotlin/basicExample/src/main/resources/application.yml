spring:
  datasource:
    url: jdbc:h2:~/data/kotlinExample
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

---